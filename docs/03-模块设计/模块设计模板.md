---
module: "{{模块英文名 (如: firewall)}}"
title: "{{模块中文名 (如: 防火墙管理)}}"
architecture: "MVVM + Clean Architecture"
status: "draft | active | refactoring | deprecated | archived"
last_updated: "{{YYYY-MM-DD}}"
version: "1.0.0"
---

# {{模块中文名}}设计说明

## 0. 文档契约与状态 (Document Contract)
- **描述对象**: 
  - [x] 当前已落地代码 (As-Is)
  - [ ] 目标架构设计 (To-Be)
  - [ ] 重构中过渡方案 (WIP)
- **维护规则**: 本文档为模块的核心契约。修改核心状态流转、公开用例接口、权限配置、持久化结构、关键异常策略时，**必须同步更新本文档**。
- **一致性原则**: 若第 3 章的路径或设计描述与当前实际代码实现不一致，应在代码变更合并（Merge）前修正本文档。代码必须服从文档设计，或文档准确反映代码现状。

## 1. 业务概述与对外接口 (Overview & Public Interfaces)
- **核心目标**: {{一句话描述该模块解决什么核心业务问题}}
- **入口路由 (Route Entry)**: {{例如：`pages/firewall/FirewallPage` 或 无}}
- **对外暴露能力 (Public APIs)**: {{其他模块可调用的 Service 核心类或全局组件，无则写“无”}}
- **业务边界**: 
  - ✅ **包含**: {{明确该模块负责的业务范围}}
  - ❌ **不包含**: {{明确不属于该模块职责的范围}}

## 2. 状态与数据流 (Data Flow & State)
> **设计原则**: 单向数据流 (View -> ViewModel -> Service -> Repository/Storage)

- **核心业务状态 (Core Business State)**:
  > *注：仅记录影响业务逻辑的关键状态，不要记录临时的 UI 变量。*
  - `{{状态变量名: 类型}}`: {{状态含义及初始值}}
- **关键流转路径**:
  - `{{触发事件}}` -> `{{处理逻辑}}` -> `{{状态变更}}` -> `{{UI同步}}`

## 3. 模块结构与组件设计 (Module Components)

### 【核心层】(Core MVVM & Domain Layers)

#### 3.1 Model & Types (核心数据模型与类型)
* **核心实体与依赖路径**: 
  > *注：请勿机械罗列所有字段，仅说明核心实体、所在真实文件，及影响业务的关键字段。*
  * `{{核心实体/DTO名称}}` 
    * **所在文件**: `{{真实路径，如 ets/models/... (全局) 或 ets/services/{{module}}/*DTO.ets (私有)}}`
    * **业务作用**: {{一句话概括实体作用}}
    * **关键字段**: `{{字段A}}` ({{用途/枚举取值}}), `{{字段B}}` ({{用途}})

#### 3.2 Service / Domain (领域业务层)
* **真实文件路径**: `{{填入核心 Service 所在的真实根路径}}`
* **核心业务用例 (Use Cases)**: 
  > *注：复杂方法请展开副作用与失败策略；简单查询方法可一行说明。*
  * `{{方法名}}({{关键参数}})`: {{业务意图}}
    * **副作用**: {{写入 RDB / 调用系统 MDM / 触发 IPC 事件 / 无}}
    * **失败策略**: {{抛出特定的业务异常 / 返回缺省结果 / 回滚旧数据}}
* **内部架构设计**:
  * **Repository**: `{{内部数据访问真实路径}}` - {{说明}}
  * **Mapper/Ports**: `{{DTO转换或端口真实路径}}` - {{说明}}

#### 3.3 ViewModel (视图模型层)
* **真实文件路径**: `{{填入 ViewModel 真实路径}}`
* **状态分发逻辑**:
  > *注：记录触发核心状态流转的方法。*
  * `{{状态流转方法名}}`: {{说明如何调用 Service 并更新第2节定义的核心状态}}

#### 3.4 View / Page (页面视图层)
* **真实文件路径**: `{{填入 Page 真实路径}}`
* **结构说明**: 
  > *注：本层仅描述页面级入口，记录页面骨架与主流程。*
  * `{{页面名称}}`: {{页面职责及主流程说明}}

#### 3.5 Components (可复用组件层)
* **真实文件路径**: `{{填入 Components 真实路径}}`
* **结构说明**: 
  > *注：本层描述复用的 UI 区块，记录输入输出与回调。*
  * `{{组件名}}`: {{UI 职责说明}}。**入参/事件**: {{如：接收 ruleList，抛出 onDelete 回调}}。

---

### 【基础设施与扩展层】(Infrastructure & Extensions)
> *注：若不适用请写“无（None）”*

#### 3.6 Storage / Database (持久化)
* **涉及修改**: {{RDB 表结构变更或 Preferences 配置真实路径}}

#### 3.7 Contracts / IPC (通信契约)
* **真实文件路径**: `{{IPC 契约真实路径}}`

#### 3.8 Constants & Utils (业务常量与工具)
* **说明**: {{本模块独有的常量或纯函数真实路径}}

#### 3.9 Ability / Runtime (系统入口)
* **说明**: {{如涉及 UIAbility 生命周期挂载真实路径}}

## 4. 异常处理与系统依赖 (Dependencies & Errors)
- **关键系统 API**: {{如 `@ohos.usbManager`，用于版本演进时评估影响面}}
- **系统权限**: {{列出 module.json5 中声明的权限}}
- **异常兜底策略**: {{记录关键场景下，系统是如何容错及在 UI 上提示的}}

## 5. 变更日志 (Changelog)
> *注：仅记录设计级变更，普通 Bugfix 或格式修正请查阅 Git Log。*

| 版本 | 日期 | 修改人 | 核心设计变更内容 (重构/新增表/用例增删) |
|---|---|---|---|
| 1.0.0 | {{日期}} | {{作者}} | 初始架构设计 |

{
  "case_id": "FW-RULE-002",
  "case_name": "删除 DNS 规则成功",
  "module": "firewall",
  "steps": [
    {
      "name": "定位目标 DNS 规则",
      "type": "mcp_action",
      "action": "find_firewall_rule",
      "params": {
        "rule_type": "dns",
        "host": "example.com"
      },
      "expected": "目标规则存在"
    },
    {
      "name": "删除目标 DNS 规则",
      "type": "mcp_action",
      "action": "delete_firewall_rule",
      "params": {
        "rule_type": "dns",
        "host": "example.com"
      },
      "expected": "规则列表中已不存在该项"
    }
  ],
  "notes": [
    "建议和 FW-RULE-001 成对执行。"
  ]
}

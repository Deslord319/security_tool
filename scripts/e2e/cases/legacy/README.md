# Legacy Cases

This directory keeps the pre-migration JSON cases for compatibility and reference.

They are no longer part of the default recursive case scan and are not referenced by the adapter suites.

Use them only when comparing old runner behavior against the declarative contract.
